""" Script to sort the data to fit my pipeline from what I got"""

# New single chips DESI → CHIP_TYPES_DESI (dropped all the files into raw).
# Liver optimization reanalysis → LIVER (dropped all the files into raw).
# Converted ESI for re-analysis → ESI (dropped all the files into raw).
